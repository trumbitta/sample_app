<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section class="well well-small">
			<h1><%= gravatar_for @user, size: 100 %>&#32;<%= @user.name %></h1>
		</section>
	</aside>
</div>